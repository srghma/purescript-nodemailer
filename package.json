{
  "private": true,
  "name": "purescript-nodemailer",
  "scripts": {
    "build": "spago build",
    "watch": "watchexec -e purs,js,yaml -- spago test",
    "clean": "rm -rfd output",
    "test": "spago test",
    "bump-version": "spago upgrade --migrate",
    "spago:publish": "spago publish"
  },
  "dependencies": {
    "nodemailer": "^6.9.15",
    "purescript": "^0.15.15",
    "purescript-psa": "^0.9.0",
    "spago": "^0.21.0"
  }
}
